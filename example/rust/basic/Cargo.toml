[package]
name = "hakodb-basic"
version = "0.1.0"
edition = "2021"
description = "Minimal Hako Rust example: open, CRUD, query, batch, transaction, watch"

[dependencies]
hakodb = { path = "../../.." }

[profile.release]
opt-level = 3

[workspace]
